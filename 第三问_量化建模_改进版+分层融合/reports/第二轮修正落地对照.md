# 第二轮修正清单落地对照（逐条）

本文档对《第三问改进版：最终修正清单》的**每一节、每一条**逐项给出落地状态与可核验文件。
清单推荐的、可直接写入论文的表述已逐字整理在 `reports/清单推荐论文表述.md`。

图例：✅ 已完全落地 ／ ⚠️ 已落地但需注意边界

---

## 一、总体判断（清单 §1 已具备的 11 项结构性修正）

| # | 清单条目 | 状态 | 证据 |
|---|---|---|---|
| 1 | 减购改为净额退款式结算 | ✅ | `code/q3.py` 的 `settle_coef()`、`solve()`、`run()`；`code/verify.py` 独立重算 |
| 2 | 增购按对应交付时段电价 1.5 倍 | ✅ | `run()` 结算段；`results/sensitivity.csv` |
| 3 | 紧急购电按对应交付时段电价 5 倍 | ✅ | 同上 |
| 4 | 交易价格统一采用电量所属交付时段分时电价 | ✅ | 全部按区间索引 `price[t]`；`results/parameters.json:settlement.price_convention` |
| 5 | 多次日内调整按相对 0:00 基准计划的净额结算 | ✅ | `solve()` 的 `q−u+v=q⁰` 约束行 |
| 6 | 6:00/12:00/18:00 严格按当时可获得信息滚动调整 | ✅ | `run()` 只执行当前区块；`checks.json` 的 `causality_future_perturbation_max_diff = 0` |
| 7 | 实际实现跨午夜 24 小时滚动优化 | ✅ | `Bank` 的 24 小时视野数组；`parameters.json:optimization_horizon` |
| 8 | SOC 跨日连续、平衡与储能动态通过检查 | ✅ | `checks.json`：`balance_max_error`、`soc_dynamics_max_error`、`cross_day_soc_max_gap` |
| 9 | 0.80 与 0.70 具有明确经济理论来源 | ✅ | `ANALYSIS_MODELING_REPORT.md` 第 5 章；`parameters.json:theory_base` |
| 10 | 主模型能产生实际减购行为 | ✅ | `checks.json` 的 `main_down_kwh`（旧解释下 `legacy_down_kwh = 0`，形成对照） |
| 11 | 无需推倒重做，只需最后修正 | ✅ | 本轮为增量修改，未改变结算主口径 |

---

## 二、P0-1 扩大午夜风险分位搜索范围

| 清单条目 | 状态 | 落地与证据 |
|---|---|---|
| 候选集合扩展为 $\{0.45,0.50,\dots,0.90\}$ | ✅ | `CALIB_ALPHA0 = (0.45,0.50,0.55,0.60,0.65,0.70,0.75,0.80,0.85,0.90)`；`CALIB_ALPHA_UPDATE = (0.50,\dots,0.80)`；共 70 组，`results/calibration.csv` |
| 若为控制计算量，至少补测 0.50、0.55 | ✅ | 已整体扩展至 0.45，含 0.50/0.55 |
| 日内分位可暂保持 $\{0.50,\dots,0.80\}$ | ✅ | 保持 7 档 |
| §2.3 参数选择原则：若 $C(0.55)<C(0.60)$ 则继续向下检查，直到最优不在边界 | ✅ | `calibrate()` 的**边界自动延展**：最优解落在边界即 ±0.05 扩展重测（`max_expand=3`）；过程记录在 `results/calibration_grid_extension.json` |
| §2.3 最终需满足“最优位于已测试区域内部，或邻近参数已证明成本回升” | ✅ | `results/checks.json` 的 `alpha0_interior = true`、`alpha_update_interior = true`；延展日志的 `at_edge` 在最终轮为空 |
| §2.4 论文表述修改 | ✅ | 逐字收录于 `清单推荐论文表述.md` 第 1 节；`RESULTS_REPORT.md` 第 4.2 节 |

---

## 三、P0-2 修正关于“跨日优化”的错误描述

| 清单条目 | 状态 | 落地与证据 |
|---|---|---|
| §3.1 代码已真正实现未来 24 小时滚动优化 | ✅ | `run()` + `Bank` 24 小时视野；18:00 的视野覆盖 18:00(today)→18:00(tomorrow) |
| §3.1 使用附件 3 中跨到次日的预测数据 | ✅ | `Bank.pv[d,k,j]`（j≥当日剩余段时为次日预报）；`data_audit.json:forecast_horizon_note` |
| §3.2 **删除**“没有把附件 3 中跨到次日的部分用于跨日优化” | ✅ | 经全库检索，模型类报告中已无该表述 |
| §3.2 替换为推荐的跨日滚动描述 | ✅ | 逐字收录于 `清单推荐论文表述.md` 第 2 节；`RESULTS_REPORT.md` 第 5 节 |
| §3.2 补充模型边界（非完整多阶段随机规划） | ✅ | `RESULTS_REPORT.md` 第 12 节“模型边界”；`ANALYSIS_MODELING_REPORT.md` 第 13 节第 1 条；`parameters.json:model_boundary` |
| 与代码一致、不夸大模型先进程度、说明 MPC 与多阶段随机规划的区别 | ✅ | 同上；并在第 5 节说明视野末段的定价处理 |

---

## 四、P0-3 重新定义 0.80/0.70 的论文地位

| 清单条目 | 状态 | 落地与证据 |
|---|---|---|
| §4.1 不称为“完整模型的理论最优分位数” | ✅ | 全库检索无该表述 |
| §4.2 统一改称“经济理论基准分位”或“无储能局部单阶段条件下的理论临界分位” | ✅ | 代码注释、`parameters.json:theory_base.name`、两份报告、README、对照文档 |
| §4.3 推荐正式表述 | ✅ | 逐字收录于 `清单推荐论文表述.md` 第 3 节；`ANALYSIS_MODELING_REPORT.md` 第 5 章章首与第 11 节 |
| 明确完整第三问还含储能跨时转移、SOC、分时电价差、0.5/1.5 倍机制、后续更新、预测误差、跨日滚动 | ✅ | `ANALYSIS_MODELING_REPORT.md` 第 5 章章首逐项列出 |

---

## 五、P1-4 / P1-5 降低 17 天校准的过拟合风险

| 清单条目 | 状态 | 落地与证据 |
|---|---|---|
| §5.1 不宜把某一组合描述为具有很强唯一最优性 | ✅ | 报告改为“邻域内形成稳定低成本区域” |
| §5.2 方案A：修改表述 | ✅ | 逐字收录于 `清单推荐论文表述.md` 第 4 节；`RESULTS_REPORT.md` 第 4.3 节末段 |
| §5.3 方案B：滚动 / 扩展窗口时序验证 | ✅ | `rolling_validation()`；结果 `results/rolling_validation.csv` |
| §5.3 折次结构：训练窗口扩展 → 随后一周验证 | ✅ | 第 1 折 训练 1/1—1/7 → 验证 1/8—1/15；第 2 折 训练 1/1—1/15 → 验证 1/16—1/23；第 3 折 训练 1/1—1/23 → 验证 1/24—1/31（`parameters.json:selection.folds`） |
| §5.3 按多个时序验证区间的平均成本选参 | ✅ | `rolling_validation.csv` 的 `mean_adjusted_cost`；主口径即由该规则选出（`parameters.json:selection.primary_rule`） |
| §5.4 目标：某一参数邻域在多个窗口中均保持较低成本 | ✅ | `region` 稳定性另由 `stability_map.csv` 全网格验证；报告第 4.4 节 |
| 单窗口规则结果作为对照并列报告 | ✅ | `variant_comparison.csv` 的 `single_window_params` 行 |

---

## 六、P1-6 / P1-7 明确岭正则参数属预测超参数，并按时序验证选择

| 清单条目 | 状态 | 落地与证据 |
|---|---|---|
| §6.1 承认 slope / weekday 非题目推导 | ✅ | 归类为**四级：预测子模型工程超参数**（`parameters.json:ridge_hyperparameters.class`） |
| §6.2 不强行制造“理论来源” | ✅ | 报告中明确“不具直接经济含义”，未赋予业务解释 |
| §6.2 作用说明：控制有限历史样本下趋势项与星期效应的估计方差 | ✅ | `ANALYSIS_MODELING_REPORT.md` 第 4.2 节；`RESULTS_REPORT.md` 第 4.7 节 |
| §6.3 推荐论文表述 | ✅ | 逐字收录于 `清单推荐论文表述.md` 第 5 节 |
| §6.4 用时间序列交叉验证选择 $\lambda^*_{\rm slope},\lambda^*_{\rm weekday}$ | ✅ | `select_ridge()` 采用**滚动原点时间序列交叉验证**：拟合窗口随原点扩展、逐日重估，按三个验证区块汇总 |
| §6.4 评价指标采用 MAE 或 RMSE | ✅ | 主判据 RMSE、次级判据 MAE；全候选与分折结果见 `results/ridge_selection.csv`、`results/ridge_cv_folds.csv` |
| §6.4 预测参数按预测准确性选，风险决策参数按最终经济成本选 | ✅ | 前者由 `select_ridge()`，后者由 `calibrate()` / `rolling_validation()`；互不混用 |
| 同时给出经济侧敏感性，避免两类准则冲突 | ✅ | `results/sensitivity.csv` 的 `slope`、`weekday` 行；`results/forecast_parameter_sensitivity.csv` 同行给出该取值的预测 RMSE |

---

## 七、完善参数来源分层（清单 §7）

| 清单条目 | 状态 | 落地与证据 |
|---|---|---|
| §7.1 第一类：题目直接给定（$m=5,a=1.5,b=0.5$、容量、功率、SOC 上下界、题给效率）且禁止调参 | ✅ | `Params` 字段说明；`ANALYSIS_MODELING_REPORT.md` 第 7 节一级表；`RESULTS_REPORT.md` 第 4.6 节 |
| §7.2 第二类：理论推导（$\alpha_0^{\rm base}=0.80$、$\alpha_h^{\rm base}=0.70$、$v_E=p_{\min}/\eta_c$） | ✅ | 第 7 节二级表；`parameters.json:theory_base`、`terminal_inventory_value_vE` |
| §7.3 第三类：历史数据校准（最终分位、误差窗口、负荷衰减、日内偏差参数） | ✅ | 第 7 节三级表；`parameters.json:selection` |
| §7.3 仅使用决策 / 评价时点之前的数据 | ✅ | 所有校准窗口早于 2025-02-01；`checks.json` 因果扰动为 0 |
| §7.4 第四类：数值或预测超参数（岭正则、$\varepsilon$、求解器容差）且明确无经济含义 | ✅ | 第 7 节四级表；`parameters.json:no_unsourced_penalties.epsilon_role` |
| §7.4 合理性通过数量级说明、预测验证、敏感性分析，而非强行赋予业务解释 | ✅ | 报告第 4.7 节与第 8 节第 6、8 条；`forecast_parameter_sensitivity.csv` |

---

## 八、保留极小数值惩罚但不称为经济惩罚（清单 §8）

| 清单条目 | 状态 | 落地与证据 |
|---|---|---|
| 采用 $\varepsilon=10^{-7}$ 并加入 $\varepsilon\sum_t(c_t+d_t)$ | ✅ | `solve()` 中 `cost[n:3*n] = p.epsilon` |
| 作用：在成本相同的多个解中优先选择吞吐更小的解 | ✅ | `parameters.json:no_unsourced_penalties.epsilon_role` |
| 明确 $\varepsilon$ 不是经济成本参数 | ✅ | 同上；`RESULTS_REPORT.md` 第 3 节末句 |
| 验证 $10^{-8},10^{-7},10^{-6}$ 下总现金成本与计划 / 调整 / 紧急购电量基本不变 | ✅ | `results/sensitivity.csv` 中 `epsilon=1e-08 / 1e-06` 两行同时给出 `total_cost`、`plan_kwh`、`final_kwh`、`emergency_kwh`、`up_kwh`、`down_kwh` |

---

## 九、不新增没有来源的调整惩罚项（清单 §9）

| 清单条目 | 状态 | 落地与证据 |
|---|---|---|
| 不建议加入 $\lambda_\Delta\sum_k\sum_t|q_t^{(k)}-q_t^{(k-1)}|$ | ✅ | 目标函数中不存在该项 |
| 除非有题目 / 手续费等明确来源，否则 $\lambda_\Delta=0$ | ✅ | `Params.lam_delta = 0.0`；`parameters.json:no_unsourced_penalties` |
| 避免对调整行为重复处罚 | ✅ | `ANALYSIS_MODELING_REPORT.md` 第 7 节“被剔除的参数” |

---

## 十、结算主口径保持不变（清单 §10）

| 清单条目 | 状态 | 落地与证据 |
|---|---|---|
| 继续采用 $C=\sum_t[p_tq_t^0+1.5p_t(q_t-q_t^0)^+-0.5p_t(q_t^0-q_t)^++5p_tz_t]$ | ✅ | `settlements='net_refund'` 为主；未改动本轮结算公式 |
| 已能产生 $q_t<q_t^0$ 的主动减购行为 | ✅ | `checks.json:main_down_kwh > 0`，且 `legacy_down_kwh = 0` |
| 旧版“减购量恒为 0”的结构性问题已修复 | ✅ | 同上 |

---

## 十一、保留的结构敏感性实验（清单 §11）

| 编号 | 清单要求 | 状态 | 落地与证据 |
|---|---|---|---|
| S1 | 旧版减购结算，用以证明“引入日内预测有价值”的核心结论不被改变 | ✅ | `variant_comparison.csv` 的 `legacy_gross_settlement`；`RESULTS_REPORT.md` 第 2.2 节给出三种解释下的节约率对照 |
| S2 | 逐次调整结算 | ✅ | `settlements='stepwise'`；`variant_comparison.csv` 的 `stepwise_settlement` 行与 `sensitivity.csv` 的 `settlements=stepwise` |
| S3 | 储能效率口径 $\eta=0.9$ 与 $\sqrt{0.9}$ | ✅ | `sensitivity.csv` 的 `eta=0.9486832980505138`（另有 0.85/0.95） |
| S4 | 历史窗口 14 / 28 / 56 天 | ✅ | `sensitivity.csv` 的 `window=14`、`window=56`（28 为主口径） |
| S5 | 风险分位邻域扰动，验证总成本与“是否需要日内更新”结论稳定 | ✅ | `stability_map.csv` 全网格 70 组；`figures/参数邻域稳定性.*`；报告第 4.4 节 |

---

## 十二、建议修改后的参数表（清单 §12 逐行）

| 参数 | 清单推荐处理 | 状态 | 本版实际 |
|---|---|---|---|
| 紧急购电倍数 $m$ | 5，不调参 | ✅ | `emergency=5`（另作 3/7 敏感性） |
| 增购倍数 $a$ | 1.5，不调参 | ✅ | `up=1.5`（另作 1.2/1.8 敏感性） |
| 减购违约倍数 $b$ | 0.5，不调参 | ✅ | `down=0.5` |
| 储能容量 | 12000 kWh，不调参 | ✅ | `capacity=12000`（另作 ±20% 结构敏感性） |
| 最大功率 | 5000 kW，不调参 | ✅ | `power=5000`（另作 ±20% 结构敏感性） |
| SOC 范围 | 10%–90%，不调参 | ✅ | `solve()` 中 `.1*p.capacity` / `.9*p.capacity` |
| $\alpha_0^{\rm base}$ | 0.80，理论推导，称理论锚点 | ✅ | `THEORY_ALPHA0` |
| $\alpha_h^{\rm base}$ | 0.70，理论推导 | ✅ | `THEORY_ALPHA_UPDATE` |
| 最终 $\alpha_0$ | 重新扩大搜索范围后确定 | ✅ | 见 `parameters.json:selection.primary_alpha0`，位于搜索区域内部 |
| 最终 $\alpha_h$ | 历史校准 | ✅ | 见 `selection.primary_alpha_update` |
| 历史误差窗口 | 28 天为主，14/28/56 敏感性 | ✅ | `window=28` + 敏感性 |
| 历史衰减尺度 | 14 天 | ✅ | `decay_days=14` + 7/28 敏感性 |
| 日内偏差窗口 | 1 小时（6 个区间） | ✅ | `bias_window=6` + 3/12 敏感性 |
| 偏差衰减 | 6 小时 | ✅ | `bias_decay=6` + 3/12 敏感性 |
| slope ridge | 时序验证或敏感性 | ✅ | 滚动原点 CV 选择 + 敏感性 |
| weekday ridge | 时序验证或敏感性 | ✅ | 同上 |
| 调整额外惩罚 | 0 | ✅ | `lam_delta=0` |
| 固定 SOC 目标惩罚 | 不建议使用（已采用 24h MPC） | ✅ | `terminal='inventory_value'`，固定口径仅作对照 |
| LP 吞吐惩罚 | $10^{-7}$，仅防退化 | ✅ | `epsilon=1e-7` |

---

## 十三 / 十四、推荐模型说明与参数来源说明（清单 §13 / §14）

| 清单条目 | 状态 | 落地与证据 |
|---|---|---|
| §13 三段式模型说明（滚动框架 / 结算 / 风险分位锚点） | ✅ | 逐字收录于 `清单推荐论文表述.md` 第 6 节；`ANALYSIS_MODELING_REPORT.md` 第 10 节 |
| §14 参数来源说明（四类参数） | ✅ | 逐字收录于 `清单推荐论文表述.md` 第 7 节；`ANALYSIS_MODELING_REPORT.md` 第 11 节 |

---

## 十五、最终修改优先级（清单 §15）

### P0：必须修改

| # | 条目 | 状态 | 证据 |
|---|---|---|---|
| 1 | 扩展 $\alpha_0$ 搜索范围，至少补测 0.50、0.55 | ✅ | 已扩展至 0.45–0.90 并含边界自动延展 |
| 2 | 删除报告中“没有使用跨日预测优化”的错误描述 | ✅ | 已删除并按清单措辞改写 |
| 3 | 0.80/0.70 统一改称“经济理论基准分位” | ✅ | 全库统一 |

### P1：强烈建议修改

| # | 条目 | 状态 | 证据 |
|---|---|---|---|
| 4 | 弱化“17 天校准得到唯一最优参数”的表述 | ✅ | 报告第 4.3 节末段 |
| 5 | 采用滚动 / 扩展窗口时序验证 | ✅ | `rolling_validation()`、`results/rolling_validation.csv` |
| 6 | 明确 slope / weekday 属预测超参数 | ✅ | 四级参数表与报告第 4.7 节 |
| 7 | 用时序验证选择这两个正则参数 | ✅ | `select_ridge()` 滚动原点 CV |

### P2：论文增强项

| # | 条目 | 状态 | 证据 |
|---|---|---|---|
| 8 | 保留旧结算口径作为结构敏感性 | ✅ | `legacy_gross_settlement` |
| 9 | 增加逐次结算对照 | ✅ | `stepwise_settlement` |
| 10 | 保留储能效率口径敏感性 | ✅ | `eta=√0.9` 等三行 |
| 11 | 展示最终风险参数邻域的成本热力图或稳定区间 | ✅ | `figures/风险分位校准曲面.*`（含最低成本 0.2% 邻域）、`figures/参数邻域稳定性.*` |
| 12 | 报告参数变化下“6+12+18 仍优于不更新”的比例 | ✅ | `parameters.json:stability.fraction_full_beats_none`；`checks.json:fraction_full_beats_none` |

---

## 十六、最终验收标准（清单 §16）

### 结算层

| 验收项 | 状态 | 证据 |
|---|---|---|
| 主结算公式与代码一致 | ✅ | `verify.py` 按主口径逐日重算，见 `results/delivery_verification.json` |
| 减购能够真实进入优化 | ✅ | `checks.json` 的 `main_down_kwh`、`main_up_kwh` 均显著为正 |
| 0.5、1.5、5 倍价格全部来自题目 | ✅ | `Params.down/up/emergency`；第 7 节一级表 |
| 交付时段价格定义一致 | ✅ | 全部按区间索引 |

### 信息层

| 验收项 | 状态 | 证据 |
|---|---|---|
| 任何决策均不使用未来真实负荷 | ✅ | `checks.json:causality_future_perturbation_max_diff = 0` |
| 任何决策均不使用尚未发布的光伏预测 | ✅ | `Bank.pv[d,k]` 只使用第 k 次发布已公开的预报；因果扰动测试 |
| 跨日部分只使用当前发布时点已给出的未来 24 小时信息 | ✅ | `data_audit.json:forecast_horizon_note`；报告第 5 节 |

### 参数层

| 验收项 | 状态 | 证据 |
|---|---|---|
| 题目参数不参与调参 | ✅ | 一级参数不在任何候选网格中 |
| 0.80/0.70 只作为理论锚点 | ✅ | `theory_base.role` |
| 最终风险参数不位于未经继续探索的搜索边界 | ✅ | `checks.json:alpha0_interior / alpha_update_interior` 均为 true |
| 预测超参数明确标注来源类别 | ✅ | `ridge_hyperparameters.class` |
| 数值参数与经济参数严格区分 | ✅ | `no_unsourced_penalties`、第 7 节一/四级表 |

### 物理层

| 验收项 | 状态 | 证据 |
|---|---|---|
| 电力平衡误差接近机器精度 | ✅ | `checks.json:balance_max_error` |
| SOC 始终位于允许范围 | ✅ | `checks.json:soc_min / soc_max`；每日 `soc_min/soc_max` 断言 |
| 跨日 SOC 连续 | ✅ | `checks.json:cross_day_soc_max_gap` |
| 不存在无意义同时充放电 | ✅ | `run()` 中 `np.minimum(c,dis).max() < 1e-5` 断言 |
| Excel 结果与内部账本一致 | ✅ | `delivery_verification.json` 各项均为 true |

### 论文层

| 验收项 | 状态 | 证据 |
|---|---|---|
| 不存在“代码已经跨日、报告却说没有跨日”的矛盾 | ✅ | 报告第 5 节已按清单措辞改写 |
| 不宣称确定性 MPC 是完整多阶段随机规划 | ✅ | 报告第 12 节、分析报告第 13 节第 1 条 |
| 不宣称 0.80/0.70 是完整动态模型严格理论最优 | ✅ | 分析报告第 5 章章首、第 11 节 |
| 不宣称所有参数都来自题目 | ✅ | 四级参数体系 |
| 明确哪些参数来自题目、理论、数据和数值计算 | ✅ | `ANALYSIS_MODELING_REPORT.md` 第 7 节；`RESULTS_REPORT.md` 第 4 章 |

---

## 十七、最终建议（清单 §17）

清单要求下一版集中完成三件事：**参数搜索边界修复 + 报告代码一致性修复 + 参数来源进一步规范**。本轮均已完成：

| 清单要求 | 状态 | 证据 |
|---|---|---|
| 参数搜索边界修复（$\alpha_0=0.60$ 曾位于搜索边界，必须继续向下补测） | ✅ | 候选扩展至 0.45 起，并加入边界自动延展；最终最优位于区域内部 |
| 报告代码一致性修复（跨日 MPC 的描述） | ✅ | 已按清单 §3.2 措辞改写 |
| 参数来源进一步规范 | ✅ | 四级参数体系 + 预测超参数按精度选择 + 清单 §14 正式表述 |

清单要求的最终逻辑链「题目经济机制 → 理论参数锚点 → 严格历史校准 → 24h 滚动 MPC → 样本外经济评价 → 结构敏感性验证」在 `RESULTS_REPORT.md` 的章节顺序与 `parameters.json` 的字段结构中一一对应。
