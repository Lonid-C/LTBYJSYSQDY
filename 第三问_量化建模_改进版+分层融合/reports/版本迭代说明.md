# 第三问交付物：版本迭代说明

本文件记录本交付包从**原版**到当前**第三轮改进版**的完整迭代过程，说明每个版本改了什么、为什么改、改了之后结果怎么变、以及哪些内容保持不变。所有数值均可在 `results/` 的账本中逐项复核。

---

## 0. 版本谱系一览

| 版本 | 名称 | 触发依据 | 版本性质 | 主策略总购电费（全更新） | 相对上版 |
|---|---|---|---|---:|---:|
| **V0** | 原版 | — | 基线交付 | 13,440,139.28 元 | — |
| **V1** | 改进版第一轮 | 《第三问模型改进建议：结算口径与参数来源说明》 | 结算口径 + 参数来源重构 | 13,435,399.87 元 | −4,739.41 元 |
| **V2** | 改进版第二轮 | 《第三问改进版：最终修正清单》 | 选参方法学 + 术语 + 敏感性 | 13,386,086.28 元 | −49,313.59 元 |
| **V3** | 改进版第三轮 | 同上清单，要求逐条完全对齐 | 方法学补齐 + 表述对齐（数值不变） | 13,386,086.28 元 | 0 元 |
| **V4** | 改进版第四轮（当前） | 用户提问「能否用遗传算法／别的方法」 | **多方法对比扩展**（增量，主模型不变） | 13,386,086.28 元 | 0 元 |

> 重要说明：**四代之间的总费用不可直接横向比较为“算法优劣”**。V1 起结算口径、优化视野、终端条件都已改变，合同条件不同，费用水平本就不同。可比的只有「同一合同内部的策略对比」与「同一目标函数内的参数校准」。V2 → V3 是纯方法学与表述补齐，数值完全一致，可直接对比。

---

## V0 — 原版（基线）

**交付形式**：`第三问_量化建模_完整交付.zip`（另一份独立文件，未包含在本包内）。

**核心设定**

| 项 | 设定 |
|---|---|
| 结算口径 | 原计划费用照付，增购按 1.5 倍，**减购在原费照付之外另加 0.5 倍罚金** |
| 优化视野 | 每个发布时刻只优化到**当日 24:00** |
| 终端条件 | 硬约束日末 SOC = 50% 容量（`E₂₄ = 0.5B`） |
| 风险分位 | 直接校准，无理论来源说明；网格 5×5=50 组，区间无理论中心 |
| 选参规则 | 1 月 15—31 日单窗口 |
| 岭正则 | 手工设定 |
| 额外惩罚项 | 含调整量惩罚、SOC 惩罚、库存保留系数 |

**关键结果**

| 指标 | 数值 |
|---|---|
| 2—12 月总购电费（全更新） | 13,440,139.28 元 |
| 不更新对照 | 14,559,779.48 元 |
| 节约 | 1,119,640.20 元（7.690%） |
| **累计减购量** | **0 kWh** |
| 累计增购量 | 351,742 kWh |
| 紧急购电量 | 41,358 kWh |
| 2 月 1 日初始库存 | 7,291.2 kWh |

**暴露出的结构性问题**

1. **减购被数学上严格支配。** 旧口径下由 100 减到 80 需付 `110p_t`，高于不调整的 `100p_t`，因此模型必然输出减购量 = 0，题目专门设置的「50% 违约价格」形同虚设。
2. **风险分位缺少理论来源**，0.80/0.70 这类取值看起来像人工试出来的。
3. **终端条件 `E₂₄ = 0.5B` 没有数据来源**，等于人为规定「SOC 越接近 50% 越好」。
4. **叠加了无来源的惩罚项**（调整量惩罚、SOC 惩罚），与题目已显式给出的 0.5/1.5/5 倍价格机制重复计价。
5. 充放电工作表「时刻」列把第二个 4 小时块误标为 24:00。

---

## V1 — 改进版第一轮

**触发依据**：《第三问模型改进建议：结算口径与参数来源说明》。

**修改目标**：修正结算口径的结构性缺陷，并把每个参数的来源讲清楚（题目给定 / 理论推导 / 数据校准 / 数值设定四级）。

### 1.1 结算口径重构（核心）

主结算公式改为相对 0:00 基准计划的**净额退款式**结算：

$$C=\sum_t\left[p_tq_t^0+1.5p_t(q_t-q_t^0)^+-0.5p_t(q_t^0-q_t)^++5p_tz_t\right].$$

即：取消部分只承担 50% 的违约成本（退回 50%），而不是在照付原费之外再罚 50%。

| 项 | V0 | V1 |
|---|---|---|
| 减购的经济含义 | 取消部分 = 原费 + 0.5 倍罚金 = 1.5 倍 | 取消部分只承担 0.5 倍违约成本 |
| 减购行为 | 恒为 0（被严格支配） | 715,987 kWh（真实进入优化） |
| 实现 | — | `settle_coef()`：净额口径系数 −b，旧口径 +b |

**为什么必须改**：只有改成净额退款，题目给出的 0.5 倍违约价格才具有决策意义，模型才能产生真实的减购行为。

### 1.2 交易电价与净额结算

| 修改 | 实现 |
|---|---|
| 交易电价统一取**该电量所属交付时段**的分时电价 | 全部按区间索引 `price[t]`，不取下单时刻价格，消除下单时点套利 |
| 多次日内调整按相对 0:00 计划的**净额结算一次** | 加约束 `q − u + v = q⁰`，只对最终净额计费（100→120→105 只按 105−100=5 结算） |

### 1.3 风险分位给出理论锚点与有限校准区间

推导无储能局部单阶段边际成本模型下的临界分位，作为**经济理论基准分位**：

$$\alpha_0^{\text{base}}=1-\frac{1}{m}=0.80,\qquad \alpha_h^{\text{base}}=1-\frac{a}{m}=1-\frac{1.5}{5}=0.70.$$

校准只在理论中心邻域内进行（V1 为 $\alpha_0\in\{0.60,\dots,0.90\}$、$\alpha_h\in\{0.50,\dots,0.80\}$，共 49 组）。

### 1.4 用经济终端库存价值取代固定 50% SOC

取消硬约束 `E₂₄ = 0.5B`，改为目标函数加入 $-v_E E_D$，其中 $v_E=p_{\min}/\eta_c$ 表示「未来重新补回 1 kWh 电池内部能量所需的最低替代购电成本」。参数来源明确，不是经验参数。

### 1.5 滚动 24 小时视野

每个发布时刻（0:00 / 6:00 / 12:00 / 18:00）优化其后 24 小时，只执行紧随的 6 小时，其余在下次发布时重新求解。

### 1.6 剔除无来源惩罚项

$$\lambda_\Delta=0,\qquad \lambda_{SOC}=0,\qquad \text{reserve}=0.$$

仅保留 $\varepsilon=10^{-7}$ 作为数值退化惩罚（在成本相同的多个最优解中选吞吐更小的解），并验证 $10^{-8}/10^{-6}$ 不改变经济结果。

### 1.7 时间窗口参数来源说明

28 天 = 4 周、14 天 = 2 周、1 小时 = 6 个 10 分钟段、6 小时 = 相邻预测发布间隔。

### 1.8 第一轮结果

| 指标 | V0 | V1 |
|---|---|---|
| 总购电费（全更新） | 13,440,139.28 元 | 13,435,399.87 元 |
| 不更新对照 | 14,559,779.48 元 | 14,969,809.57 元 |
| 节约 | 7.690% | **10.250%** |
| 累计减购量 | 0 kWh | **735,114 kWh** |
| 累计增购量 | 351,742 kWh | 563,509 kWh |
| 紧急购电量 | 41,358 kWh | 81,932 kWh |
| 选定风险参数 | α₀=0.65, αₕ=0.50 | α₀=0.60, αₕ=0.55 |

### 1.9 本轮暴露的新问题

1. 第一轮最优解 **α₀=0.60 恰好落在搜索区间下边界**，只能说明「已测候选中 0.60 最好」，不能说明邻域已覆盖完整。
2. 报告中残留「没有把附件 3 跨到次日的部分用于跨日优化」的表述，与代码已实现的跨午夜 24 小时滚动视野**相互矛盾**。
3. 0.80/0.70 被直接称为「理论值」，容易被误解为完整动态模型的理论最优值。
4. 仅以 17 天单窗口选参，存在过拟合风险；岭正则系数被手工设定，且未说明其参数类别。

---

## V2 — 改进版第二轮

**触发依据**：《第三问改进版：最终修正清单》（P0/P1/P2 三级）。

**修改目标**：修复选参边界截断、消除报告与代码矛盾、重新定位理论分位的论文地位，并把「唯一最优」的表述弱化为「稳定低成本邻域」。

### 2.1 P0-1 扩大风险分位搜索范围 + 边界自动延展

- 候选集合扩为 $\alpha_0\in\{0.45,0.50,\dots,0.90\}$（10 档）× $\alpha_h\in\{0.50,\dots,0.80\}$（7 档）= **70 组**。
- `calibrate()` 新增**边界自动延展**：若最优解仍落在边界，就继续 ±0.05 扩展重测，直至最优位于已测试区域内部。
- 结果：最优 (α₀, αₕ) = **(0.60, 0.60)** 已位于区域内部，`checks.json` 的 `alpha0_interior` / `alpha_update_interior` 均为 `true`。
- 证据：`results/calibration.csv`、`results/calibration_grid_extension.json`。

### 2.2 P0-2 删除与代码矛盾的描述

删除「没有使用跨日预测优化」的旧表述，改写为与代码一致的描述：各发布时刻采用 **24 小时滚动视野**，跨午夜窗口使用**当前决策时点已发布的**次日预测部分。同时补充模型边界声明：**仍是确定性滚动优化，不是完整多阶段随机规划**。

### 2.3 P0-3 术语修正：经济理论基准分位

全库统一改称 **「经济理论基准分位」（无储能局部单阶段条件下的理论临界分位）**，明确仅作为完整储能滚动优化模型的**理论锚点**，不再称完整模型的理论最优值。直接使用该锚点而不校准的费用为 13,478,826.78 元（相对主口径 +0.692%）。

### 2.4 P1-4/P1-5 弱化唯一最优 + 滚动多窗口时序验证

- 新增 `rolling_validation()`：在 3 个评价期之前的连续窗口上回放候选参数，按三个验证区块的**平均库存校正成本**选参。
- 折次结构：第 1 折 训练 1/1–1/7 → 验证 1/8–1/15；第 2 折 训练扩展至 1/15 → 验证 1/16–1/23；第 3 折 训练扩展至 1/23 → 验证 1/24–1/31。
- 主口径即由该规则选出的 (0.60, 0.60)；单窗口规则选出的 (0.60, 0.55) 结果 **+0.320%**，作为对照并列报告。
- 报告措辞改为「风险参数**邻域内形成稳定低成本区域**」，不再断言某点唯一最优。
- 证据：`results/rolling_validation.csv`、`parameters.json:selection.folds`。

### 2.5 P1-6/P1-7 岭正则系数归类为预测超参数并按精度选择

- 归类为**四级：预测子模型工程超参数**，明确其**无经济含义**，不赋予不存在的业务解释。
- 用 `select_ridge()` 按**日间负荷预测精度**选择，而不是按账单：在评价期之前的窗口上选 RMSE 最小者。
- 得到 $\lambda_{\text{slope}}=10^{6}$、$\lambda_{\text{weekday}}=0.00625$。
- 两类参数彻底分开：**预测模型参数按预测准确性选，风险决策参数按最终经济成本选**。
- 如实说明：精度准则把趋势项正则推到上限（等价于取消局部线性趋势项），说明该窗口内局部趋势对预测精度无正贡献。

### 2.6 P2-9 新增逐次调整结算对照

新增 `settlements='stepwise'`（基准改为上一次有效计划，逐次计费）。三种结算解释并列对比：

| 结算解释 | 总费 / 元 | 累计减购 / kWh | 相对主口径 |
|---|---:|---:|---:|
| 主口径：净额退款结算 | 13,386,086.28 | 715,987 | 0.000% |
| 旧解释：原费照付＋0.5 倍罚金 | 13,448,630.15 | 0 | +0.467% |
| 逐次调整结算 | 13,555,574.64 | 958,315 | +1.266% |

核心结论对结算解释不敏感：不更新策略不产生任何调整，三种解释下费用完全相同。

### 2.7 P2-11/P2-12 参数邻域稳定性

对全部 70 个风险参数组合在评价期逐一回放：**100% 的参数组合下「6/12/18 全更新」仍优于不更新**，最低成本 0.2% 邻域占 14.3%。新增 `figures/参数邻域稳定性.*` 与 `results/stability_map.csv`。

### 2.8 第二轮结果

| 指标 | V1 | V2 |
|---|---|---|
| 总购电费（全更新） | 13,435,399.87 元 | **13,386,086.28 元** |
| 不更新对照 | 14,969,809.57 元 | 14,865,248.28 元 |
| 节约 | 10.250% | 9.950% |
| 累计减购量 | 735,114 kWh | 715,987 kWh |
| 紧急购电量 | 81,932 kWh | **65,262 kWh** |
| 选定风险参数 | α₀=0.60, αₕ=0.55（单窗口） | α₀=0.60, αₕ=0.60（多窗口） |
| 岭正则 | 手工 0.2 / 0.05 | 按精度选 10⁶ / 0.00625 |
| 搜索边界 | 最优落在下边界 | **最优位于区域内部** |

---

## V3 — 改进版第三轮（当前版本）

**触发依据**：同一份《最终修正清单》，用户要求**逐条完全对齐**。

**修改目标**：把前两轮中「实质已做、但表述或方法学细节未完全对齐清单」的三处补齐。**本轮不改变任何数值结论**，选参与全部结果与 V2 完全相同。

### 3.1 岭正则改为真正的滚动原点时间序列交叉验证（清单 §6.4）

- `select_ridge()` 由「逐日重估的时序校核」明确实现为**滚动原点时间序列交叉验证**：对每个候选组合，**拟合窗口随预测原点逐日滚动扩展、每次只用该日之前的数据拟合**，再预测该日全天负荷；误差按三个验证区块汇总。
- 主判据 RMSE、次级判据 MAE。
- 新增 `results/ridge_cv_folds.csv`（56 候选 × 3 折的完整分折结果），并在报告中新增分折明细表。
- 选中值的分折表现（随训练窗口扩展而改善，正是 rolling-origin CV 应有的形态）：

| 折次 | 拟合窗口 | 验证区块 | MAE / kW | RMSE / kW |
|---|---|---|---:|---:|
| 1 | 2025-01-01/07 | 2025-01-08/15 | 142.50 | 179.16 |
| 2 | 2025-01-01/15 | 2025-01-16/23 | 121.15 | 150.76 |
| 3 | 2025-01-01/23 | 2025-01-24/31 | 119.27 | 147.51 |

### 3.2 验证折次显式化（清单 §5.3 方案 B）

`rolling_validation()` 的折次结构写入 `parameters.json:selection.folds`，并在两份报告中显式列表：

| 折次 | 训练窗口（扩展） | 验证窗口 |
|---|---|---|
| 1 | 2025-01-01 → 01-07 | 2025-01-08 → 01-15 |
| 2 | 2025-01-01 → 01-15 | 2025-01-16 → 01-23 |
| 3 | 2025-01-01 → 01-23 | 2025-01-24 → 01-31 |

按三个验证区块的**平均**库存校正成本选参。

### 3.3 清单推荐表述逐字收录

新建 `reports/清单推荐论文表述.md`，把清单 §2.4、§3.2、§4.3、§5.2、§6.3、§13、§14 给出的推荐文本逐字整理（仅把 pandoc 导出格式规范为 LaTeX，**文字未改**，可直接粘进论文）；同时把 §13/§14 原文写入 `ANALYSIS_MODELING_REPORT.md` 第 10/11 节，§4.3 原文写入第 5 章章首。

### 3.4 量化「最优邻域」的程度（清单 §5.1）

在报告校准章节新增量化证据：主口径选参 (0.60, 0.60) 与其**八邻域**候选的校准费用最大相差 **0.646%**，所选点相对邻域最低值仅差 **0.027%**——用数字支撑「不宜称某组合唯一最优」的表述。

### 3.5 逐条对照文档重写

`reports/第二轮修正落地对照.md` 重写为**逐节逐条**表格，覆盖清单 §1–§17 的每一项，含 §12 参数表 19 行逐行、§15 优先级 12 项、§16 验收标准五层（结算 / 信息 / 参数 / 物理 / 论文），每条标注状态与证据文件。

### 3.6 本轮结果

**数值与 V2 完全一致**（α₀=0.60、αₕ=0.60；全更新 13,386,086.28 元、节约 9.950%）。这说明本轮是方法学与表述层面的补齐，**没有为了对齐清单而改动数值结论**。

---

## V4 — 改进版第四轮（当前版本）：多方法对比扩展

### 4.1 触发依据

用户在 V3 交付后提出两个连续问题：

1. 「这道题有没有办法用遗传算法来做，或者能不能用到别的方法」
2. 「把合适的模型放到这道题的不同部分去做，多做几种并进行对比，从各个维度对比后给出优劣」

这不再是修正类问题，而是**方法论扩展**问题。因此本轮不改变主模型任何数值，而是在其之外建立一套可比的替代方法实验。

### 4.2 修改目标

- 让「为什么用 LP」从一句断言变成**有实测数据支撑的论证**。
- 明确遗传算法在本题**能用的位置**与**不能用的位置**，且用实验而非经验说话。
- 给出多方法的多维优劣矩阵，支撑论文的方法章节定位。

### 4.3 逐条改动

**新增 `code/method_comparison.py`**（对比实验框架）

- 复用 `q3` 的 `Bank`／`execute`／结算口径，全部方法在同一预测、同一结算规则、同一初始条件下回测，保证可比。
- 新增 `run_generic()`：通用回测驱动器，已验证与 `q3.run` **逐位一致**（费用差 < 1e-6）。
- 新增 `evaluate_plan()`：把任意购电计划按主口径物理执行并计价，可暴露「名义最优但物理不可执行」的问题。

**新增求解器**

| 方法 | 实现 | 用途 |
|---|---|---|
| LP | `q3.solve`（HiGHS） | 基准，全局最优 |
| DP | `dp_solve_clean`（后向值函数 + 精确离网前向传递） | 独立验证 LP 最优性 |
| 规则策略 | `rule_plan`（6 参数阈值） | 可解释下界 |
| GA-144 | 直接编码 144 维购电计划 | 对照失败案例 |
| NSGA-II | 优化 6 个规则参数 | 成本—寿命 Pareto |
| 分位谱 | α₀ ∈ {0.45…0.95} | 成本—风险权衡（鲁棒思想） |

**DP 实现中的两个关键修正**（值得记录）

1. 初版 DP 从 `initial` 直接吸附到最近网格点，导致**人为紧急购电 39 kWh**。改为「后向值函数在网格上求解 → 前向传递按精确 off-grid 初值执行」，紧急电量降为 **0**。
2. 规则策略在**光伏盈余（风险净负荷为负）**时段会返回未经钳制的负购电计划。已在放电分支补 `np.maximum(..., 0)`；GA 适应度同时加入 `1e12` 罚值保护，避免单个不可行个体中断整轮进化。

**新增 `code/method_report.py`**：生成 3 张对比图与 `reports/方法对比报告.md`。

### 4.4 本轮结果

**L1 — 单阶段最优性**（10 个样本日，对照 LP 全局最优）

| 方法 | 平均间隙 | 中位 | 最差 | 单次耗时 | 紧急电量 |
|---|---:|---:|---:|---:|---:|
| LP（基准） | 0.000% | 0.000% | 0.000% | 4.9 ms | 0 |
| **DP（ngrid=160）** | **0.530%** | 0.493% | 1.057% | 19.5 ms | 0 |
| DP（ngrid=80） | 1.007% | 0.933% | 1.947% | 8.4 ms | 0 |
| DP（ngrid=40） | 2.167% | 1.664% | 4.421% | 5.2 ms | 0 |
| **GA（直接编码 144 维）** | **20.314%** | 13.761% | **41.510%** | 604 ms | 13.63 kWh |
| 规则（手工阈值） | 48.990% | 40.563% | 93.406% | <0.1 ms | 0 |

**关键证据 1**：DP 间隙随网格加密呈 **O(1/N)** 单调收敛（2.167 → 1.007 → 0.530%，接近 4:2:1），这是离散化误差的标准行为，构成对 LP 最优性的独立验证。

**关键证据 2**：GA 直接编码 144 维，在**给足评估预算**（604 ms/日，是 LP 的 123 倍）的情况下仍平均差 20.31%，且有 13.63 kWh 的紧急电量（违反物理约束）。这说明瓶颈**不是算力，而是搜索空间结构**。

**L2 — 全年策略回测**（334 天，2/1 起）

| 方法 | 总费 / 元 | 相对 LP | 紧急电量 / kWh | 等效循环 | 耗时 / s |
|---|---:|---:|---:|---:|---:|
| **LP-MPC（基准·精确）** | **13,386,086** | — | 65,262 | 222.0 | 8.33 |
| DP-MPC（SOC 离散） | 13,508,497 | +0.914% | 64,287 | 217.1 | 27.38 |
| RULE（GA 调参） | 15,029,238 | +12.255% | 57,349 | 131.0 | 0.29 |
| LP（不更新，仅 0:00 计划） | 14,865,248 | +11.050% | 388,254 | 215.9 | 1.20 |
| RULE（不更新） | 15,909,820 | +18.831% | 130,254 | 93.5 | 0.07 |
| RULE（手工阈值） | 21,297,070 | +59.080% | 1,028,986 | 145.1 | 0.29 |

**关键证据 3**：GA 调参把规则策略从 +59.08% 拉到 **+12.26%**，提升 46.8 个百分点 —— 这是 GA 在本题**真正有效**的用法（低维策略参数寻优），与 L1 中直接编码 144 维的失败形成鲜明对照。

**关键证据 4**：「引入日内更新有价值」在 LP（+11.05%）与规则（+18.83%）两种求解器下**都成立**，说明这是问题结构本身的性质，不依赖具体算法。

**L3 — 多目标**

- **成本—寿命 Pareto**（NSGA-II，11 个前沿点）：等效循环 **1.1 ～ 8.3 次**，费用 137.1 万 ～ 182.8 万（校准窗口）。这是 LP 单目标给不了的信息。
- **成本—风险谱**：α₀=0.60 处 CVaR95 最低（63,668 元），两端均变差 —— 分位过低推高紧急购电（72,193 → 44,856 kWh），过高推高正常购电（13,386,086 → 14,009,228 元）。**权衡非单调**。

### 4.5 未改动项

主模型全部数值、参数、结算口径、图表均未改动。V4 是**纯增量扩展**：新增 2 个程序、3 张图、1 份报告、11 个结果 CSV，原有产物逐位不变。

---

## 4. 跨版本指标总表

| 指标 | V0 原版 | V1 第一轮 | V2 第二轮 | V3 第三轮 | V4 第四轮（当前） |
|---|---|---|---|---|---|
| 结算口径 | 原费照付＋减购另加 0.5 倍罚金 | 净额退款（取消部分承担 0.5 倍） | 同 V1，新增逐次结算对照 | 同 V2 | 同 V3 |
| 优化视野 | 仅到当日 24:00 | 发布时刻 → 次日同一时刻（24h 滚动） | 同 V1，报告与代码一致 | 同 V2 | 同 V3 |
| 终端条件 | 人工固定 E₂₄ = 0.5B | 经济库存价值 −v_E·E_D | 同 V1 | 同 V2 | 同 V3 |
| 风险分位命名 | 无理论来源 | 称「理论值」 | **经济理论基准分位**（理论锚点） | 同 V2 | 同 V3 |
| 校准网格 | 5×5=50 组，无理论中心 | 7×7=49 组，最优落下边界 | 10×7=70 组 + 边界自动延展，最优在内部 | 同 V2 | 同 V3 |
| 选参规则 | 1/15—31 单窗口 | 1/15—31 单窗口 | **滚动多窗口时序验证**（3 折平均成本） | 同 V2，折次显式化 | 同 V3 |
| 选定风险参数 | α₀=0.65, αₕ=0.50 | α₀=0.60, αₕ=0.55 | α₀=0.60, αₕ=0.60 | 同 V2 | 同 V3 |
| 岭正则 | 手工设定 | 手工 0.2 / 0.05 | 按预测精度选择 10⁶ / 0.00625 | 改为**滚动原点时序 CV** 选择 | 同 V3 |
| 总购电费（全更新） | 13,440,139.28 元 | 13,435,399.87 元 | 13,386,086.28 元 | 13,386,086.28 元 | 13,386,086.28 元 |
| 不更新对照 | 14,559,779.48 元 | 14,969,809.57 元 | 14,865,248.28 元 | 14,865,248.28 元 | 14,865,248.28 元 |
| 节约 | 7.690% | 10.250% | 9.950% | 9.950% | 9.950% |
| 累计减购量 | 0 kWh | 735,114 kWh | 715,987 kWh | 715,987 kWh | 715,987 kWh |
| 累计增购量 | 351,742 kWh | 563,509 kWh | 606,724 kWh | 606,724 kWh | 606,724 kWh |
| 紧急购电量 | 41,358 kWh | 81,932 kWh | 65,262 kWh | 65,262 kWh | 65,262 kWh |
| 2/1 初始库存 | 7,291.2 kWh | 1,904.7 kWh | 1,520.7 kWh | 1,520.7 kWh | 1,520.7 kWh |
| 7 日区块 bootstrap 95% 区间 | [625,867, 1,211,757] 元 | [1,137,462, 2,016,075] 元 | [1,080,157, 1,958,331] 元 | [1,080,157, 1,958,331] 元 | [1,080,157, 1,958,331] 元 |
| 充放电表「时刻」标记 | 第二块误标 24:00 | 仅末块标 24:00，首块标 00:00 | 同 V1 | 同 V2 | 同 V3 |
| 结算重算自检 | 按旧口径独立重算 | 按主口径逐日重算 + 校验 v_E | 同 V1 | 同 V2 | 同 V3 |
| 替代方法对比 | — | — | — | — | **DP／GA／规则／NSGA-II 三层对比** |
| 报告图表数 | 7 | 7 | 7 | 7 | **10** |

---

## 5. 各版本「未改动 / 保留」的部分

以下内容自 V1 确立后被后续版本完整保留，未再改动：

- **因果信息约束**：任何预测只使用决策时点之前已可获得的信息；`checks.json` 中未来数据扰动后预测变化为 `0.000e+00`。
- 分位数残差库取「当前日之前完整历史日」的残差，并按**提前量**组织。
- 8 种日内更新组合的消融框架、条件边际价值、纯光伏信息消融、区块 bootstrap。
- 逐段账本、逐日账本、约束断言与提交表结构。
- 数据时段不平移；原结果模板另存于 `data/result3_原模板.xlsx`。

**跨版本始终成立的结论**：

1. 「引入日内预测（6/12/18 更新）有价值」——V0 起即为 100%，V2 在 70 组参数下仍为 100%。
2. 结算口径、终端条件、优化视野、选参规则的变化**都不改变**上述主结论。

---

## 6. 版本可复现性

每版的关键数值均由 `results/` 中的账本文件与 `checks.json` 固化，可独立复核：

| 版本 | 可核验文件 |
|---|---|
| V0 | 原版交付包 `results/`（另份文件） |
| V1 | 本包内已无独立 V1 账本，指标保留于 `reports/改进前后对比.md` |
| V2/V3 | `results/parameters.json`、`checks.json`、`variant_comparison.csv`、`stability_map.csv`、`rolling_validation.csv`、`ridge_selection.csv`、`ridge_cv_folds.csv`、`delivery_verification.json` |

复现命令（V3 完整流水线）：

```bash
python code/q3.py               # 主模型：校准 + 边界延展 + 多窗口验证 + 邻域稳定性 + 敏感性
python code/extra_sensitivity.py
python code/pure_release.py
python code/deliver.py          # 图表 + 两份报告
python code/export_xlsx.py      # result3.xlsx
python code/verify.py           # 独立复核
python code/compare_versions.py # 三代对照
node   code/build_report.mjs    # 离线 HTML
```

---

## 7. 迭代逻辑链

五个版本共同落实了这条从经济机制到样本外验证的完整链路：

$$\text{题目经济机制}\ \rightarrow\ \text{理论参数锚点}\ \rightarrow\ \text{严格历史校准}\ \rightarrow\ \text{24h 滚动 MPC}\ \rightarrow\ \text{样本外经济评价}\ \rightarrow\ \text{结构敏感性验证}\ \rightarrow\ \text{方法学横向定位}$$

- **V0 → V1**：修正结算口径（让 0.5 倍违约价格真正起作用）、补齐参数来源（四级参数体系）。
- **V1 → V2**：修复选参边界截断、消除报告与代码矛盾、重新定位理论分位、把「唯一最优」改为「稳定低成本邻域」。
- **V2 → V3**：把方法学细节（rolling-origin CV、扩展窗口折次）与清单推荐表述**逐条对齐**，不改变数值。
- **V3 → V4**：在主模型之外建立**替代方法的可比实验**（DP 验证最优性、GA 定位适用场景、NSGA-II 提供 Pareto），把「为什么用 LP」从断言升级为实测论证，主模型数值不变。

前四步是「把一个问题做对」，V4 是「说明为什么这个做法是对的、以及它还不是什么」。
