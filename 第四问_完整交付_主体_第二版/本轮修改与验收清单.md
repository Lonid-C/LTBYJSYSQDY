# 第四问交付包　本轮修改与验收清单

**版本 variant_id：`Q4-e2549ddb99`**　（上一版 `Q4-3e5e1b6117`）

一句话结论：本轮不只改论文措辞——**模型定义、程序实现、统计重算、论文文字与最终交付文件五者已统一口径，并可用包内脚本互相复核**。
验收入口：`python code/q4_check_docs.py`（14,749 项检查）与 `python code/q4_check_reproduce.py --days 12`。

---

## 一、P0 项处置

| 复核项 | 结论 | 证据 |
|---|---|---|
| 逐次结算 `detail=False` 漏计 | 上一版**已修复**，本轮复核通过：四组 `detail=False/True` 回归误差全为 **0.0**；逐笔账本与逐日账单最大差 3.6e-12 元；逐次合同下**已重新优化决策**（14,080,126.50／14,078,771.73 元），并非只改末端账单 | `results4/q4_repair_checks.json`、`code/q4_repair_run.py` |
| Bootstrap 合法块起点与样本长度 | 上一版**已修复**，本轮复核通过：起点上界 `n-Lb+1`、块数 `ceil(n/Lb)`、截断到恰好 334；与正文 CVaR 调用同一统计函数 | `code/q4_statistics.py`、`results4/q4_cvar_bootstrap.csv` 的 `method` 字段 |
| shrink 范围口径自相矛盾 | 按包内可核验记录统一为**口径 B**：参数**取值**在评价期回放前冻结，但参数**范围与模型结构**曾结合历史全年回放的稳定性表现修订，故**不构成严格独立盲测** | 依据 `reports4/第四问_波动电价分层融合报告.md`、`README_Q4.md` 的原始记载；论文 H9、4.6.5 第 1 条、4.7.5、4.8 已同步 |
| `479,390.29` 与正确值不一致 | 全包统一为 **479,390.09**（权威来源 `q4_selected_policy.json` 的 `Q4-2_quantile.fitness`），并由校验器拦截旧值 | `code/q4_check_docs.py` 的 `FORBIDDEN` |
| "分段线性凸"术语错误 | 全包统一为**分段线性凹**；4.4.5 证明段按"有限个仿射函数的逐点最小值 / 非凹时只得凹包络 / 需 0-1 变量转 MILP"重写；删去孤立英文 `representation`；"单纯形法多项式时间""NP 困难的 MILP"改为严谨表述；插图标题与出图代码同步 | 论文 4.4.5、`code/q4_figures_paper.py` |
| 1.5 倍增购的 `57.6%` 口径不清 | **旧值作废并重算**。新增 `code/q4_release_pricing.py`，按"发布时点—可调整交付时段"逐对计算：**72,144** 个有效配对中 **19,412**（**26.91%**）满足 `1.5·p_release < p_delivery`；6:00 4.37%、12:00 **74.16%**、18:00 0.00% | `results4/q4_release_pricing_pairs.csv`、`q4_release_pricing.json`、新增插图 `Q4_下单计价配对` |

> 关于 `57.6%`：该数值既非有效配对结果，也复现不出——未配对口径（0.6648 与全年所有时段比较）实际为 **54.52%**。故整体作废，不保留。

## 二、P1 / P2 项处置

- **Q4-2 Excel 费用列**：本版"全天购电费（含紧急购电）"末列合计 15,128,648.30 元 = 当日总现金费用，口径正确。本轮补上两簿「各表末列口径」与「核对方式」说明行，并把**费用列核对**写进 `q4_verify.py`（末列合计与汇总页四个分项对逐日账本的差**均为 0.0 元**，记入 `q4_verification.json` 的 `*_sheet_cost_detail`）——不再以"计划量差 0 kWh"充当费用列正确的证据。
- **配置唯一入口**：原包缺少 `results/parameters.json`（第三问产物），导致 `q4_freeze.py` / `q4_verify.py` / `q4_lower_bound.py` **在包内根本跑不起来**。本轮把这组继承参数固化为 `config/q3_inherited_parameters.json`，由 `q4_config.inherited_parameters()` 唯一读取，并用 `q4_check_reproduce.py` 证明它**复现冻结账本**（12 天逐列最大误差 5e-7，来自 CSV 10 位小数取整）。
- **指纹与版本闭环**：指纹清单扩展为**代码 43 / 数据 4 / 配置 2 / 插图 19 / 结果 16 / 交付物 2**；插图与配置文件首次纳入。`q4_freeze.py` 冻结后自检，`q4_check_docs.py` 复检。
- **HTML 阅读稿**：原 HTML 为手工维护，已与 Markdown 脱节——**4 处插图引用指向不存在的文件并被静默换成了另一张图**。新增 `code/q4_build_paper_html.py`，HTML 改为由 `.md` 确定性生成，插图缺失即报错退出；校验器会重新生成并比对，不同步即失败。
- **插图**：正文引用的 14 张图全部存在且图号连续（图 4-1 … 4-14）；出图脚本自动同步到 `论文交付/2_论文部分/figures` 与 `3_可视化部分/{PNG,PDF}`，无需手工拷贝。
- **信息价值命名边界**：4.6.5 第 4 条改写，并注明完美电价对照只替换**决策阶段**输入的价格中心路径（`price_mode='perfect'` 只改写 `price_paths()`），**结算仍按真实实现价格**——已核对代码确认。
- **NSGA-II**：改为"在本文参数范围、搜索预算与数值精度下未观察到稳定可辨识的成本—CVaR 权衡"，明确不构成对全参数空间 Pareto 结构的理论证明。
- **4.6.5 重组**：`诚实性条款` → `4.6.5 模型解释边界与稳健性说明` 六条；全包不再出现"诚实性条款第 N 条"。
- **语言统一**：`迭加→叠加`、`開始→开始`、`epoch` 首现改"决策时点（epoch）"后文统一、`memetic（文化基因）→模因算法`、GA/DE/SA"并行/轮流"与"三选一"的冲突按实际代码统一为**三选一（实际用 GA）**、"单峰"改为"所测试网格内的最优点位于内部"、策略标签（B0/B1/M2/H2/H3/U2/U3/W2/W3/A3/X3）在正文、图表与结果文件间统一。
- **下界三口径**：库存校正口径下界 / 该解的现金分量 / 现金口径下界三者在论文、底稿、解答册中一致分开陈述（数据底稿原先把第二个误标为现金口径下界，已改）。

## 三、验收清单执行结果

### 代码与结果
- [x] `detail=False` 与 `detail=True` 费用完全一致（误差 0.0）
- [x] 逐次调整费不低于同一交易链的净额调整费
- [x] 逐次结算已按合同口径**重新优化**，非仅末端改账单
- [x] Bootstrap 覆盖最后一个合法块起点，每次样本长度恰为 334
- [x] CVaR 与 bootstrap 调用同一统计函数
- [x] `57.6%` 已按有效配对重算为 **26.91%**
- [x] 论文数字与最新结果表逐项一致（校验器 A 组）

### 论文
- [x] 无 `479,390.29`、`479,320`；统一 `479,390.09`
- [x] 终端价值函数统一称"分段线性凹函数"
- [x] 不再出现"Q4-2 没有 recourse"
- [x] 不把回放费用差称为严格 VSS/EVPI
- [x] 不把有限搜索下的单点前沿写成理论结论
- [x] shrink 冻结时间与实际选参流程一致（口径 B）
- [x] `1.42%`、`0.46%` 均写明相对各自分支 B0（α=0.60）基线
- [x] 删除"7 位有效数字""必须规避过拟合""与条款本意相反"等表述
- [x] Bootstrap 区间为修复后结果，并说明局部平稳近似前提
- [x] 现金下界、库存校正下界与特定解的现金分量严格区分

### 交付一致性
- [x] Excel 费用列含义与实际数值一致，且费用列核对写入验证文件
- [x] 报告、Excel、CSV、JSON、图表使用同一主策略与同一版数字
- [x] 配置为真实唯一入口（`config/q3_inherited_parameters.json`），且经复现性抽查证明
- [x] 代码 / 配置 / 插图 / 结果 / 交付物指纹在冻结后重新生成
- [x] 打包文件逐项通过指纹校验
- [x] 旧错误结果与旧口径措辞无残留（校验器 B 组按行扫描，写作纪律引用行豁免）

## 四、仍需知悉的限制（未改、也不应改）

1. Q4-2 放宽范围候选解的评价期费用 **15,807,845 元**沿用原实验记录值，本版未重跑该算例。
2. 评价期结果**不构成独立盲测**（口径 B），只用于同一历史样本上的回放稳定性比较。
3. 两阶段 LP 的情景内决策使用完整情景路径，前视近似的误差量级未单独量化。
4. 数据只有一个年份，结论应表述为"历史模拟下的稳健性"，不是对未来年份的收益保证。
