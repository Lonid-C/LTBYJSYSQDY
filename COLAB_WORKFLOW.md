# Colab 同步约定

本项目默认使用本机 `colab` CLI 连接 Google Colab。当前约定的会话名为 `cumcm-2026-last-dance`。

## 每次运行的标准流程

1. 将本地项目打包，排除 `.git/`、`tmp/`、缓存和凭据。
2. 创建具名 Colab 会话，并把项目解压到 `/content`。
3. 在远端 `/content` 环境中执行 notebook；执行时不得读取未来数据。
4. 下载带完整输出的 notebook，并同步新增的 `results/`、`figures/` 和结果工作簿。
5. 检查所有代码单元已执行、没有错误输出、关键数字与结果文件一致。
6. 停止会话，避免空闲消耗计算额度。

## 数据归属

- 本地工作区是唯一长期保存位置。
- Colab VM 文件系统是临时运行环境，停止会话后不保证保留。
- `.ipynb` 必须保存代码、Markdown 解释和完整输出，不交付只有代码没有输出的版本。
- OAuth token、会话配置和 Google 凭据不得写入项目目录或打包上传。

